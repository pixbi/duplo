{
  "name": "duplo",
  "version": "1.1.4",
  "homepage": "https://github.com/pixbi/duplo",
  "scripts": {
    "prepublish": "cabal sandbox init; cabal configure; cabal install --only-dependencies -j8 --reinstall --force-reinstall; cabal build"
  },
  "bin": {
    "duplo": "util/run.sh"
  },
  "dependencies": {
    "http-server": "^0.7.2",
    "jade": "^1.7.0",
    "stylus": "^0.49.1"
  },
  "license": "MIT"
}
